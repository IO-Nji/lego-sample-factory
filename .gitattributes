# This repository does not use Git LFS
# Explicitly disable LFS to prevent pull errors for users who have Git LFS globally configured

# Set line endings for cross-platform compatibility
* text=auto

# Maven wrapper scripts should have LF line endings on all platforms
**/mvnw text eol=lf
*.sh text eol=lf

# Windows batch files should have CRLF line endings
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary files
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
