spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # User Service Routes
        - id: users_route
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
        - id: auth_route
          uri: lb://user-service
          predicates:
            - Path=/api/auth/**
            
        # Masterdata Service Routes
        - id: masterdata_route
          uri: lb://masterdata-service
          predicates:
            - Path=/api/masterdata/**
        - id: workstations_route
          uri: lb://masterdata-service
          predicates:
            - Path=/api/workstations/**
        - id: products_route
          uri: lb://masterdata-service
          predicates:
            - Path=/api/products/**
            
        # Inventory Service Routes
        - id: inventory_route
          uri: lb://inventory-service
          predicates:
            - Path=/api/inventory/**
        - id: stock_route
          uri: lb://inventory-service
          predicates:
            - Path=/api/stock/**
            
        # Order Processing Service Routes (Multi-domain)
        - id: customer_orders_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/customer-orders/**
        - id: warehouse_orders_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/warehouse-orders/**
        - id: production_orders_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/production-orders/**
        - id: production_planning_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/production-planning/**
        - id: production_control_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/production-control-orders/**
        - id: assembly_control_route
          uri: lb://order-processing-service
          predicates:
            - Path=/api/assembly-control-orders/**
            
        # SIMAL Integration Service Routes
        - id: simal_route
          uri: lb://simal-integration-service
          predicates:
            - Path=/api/simal/**

      # Global CORS Configuration for Frontend Integration
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:5173"
              - "http://localhost:5174"
              - "${FRONTEND_URL:http://localhost}"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

server:
  port: 8080

# Load Balancer Configuration
eureka:
  client:
    enabled: false

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true

# Logging Configuration
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web.cors: DEBUG
    com.lego.factory.gateway: DEBUG